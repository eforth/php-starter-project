<?php

include("../classes/User.php");

/**
 * Authenticates the user given the username and password from the login form.
 *
 * @param $username
 * @param $password
 * @return User|null
 */
function authenticate($username, $password): ?User {
    return null;
}